package ru.math.solver;

/** Что содержит шаг решения — определяет способ отображения. */
public enum StepKind {
    /** Уравнение — рендерится «как в тетради». */
    EQUATION,
    /** Готовый текст: ОДЗ, ответ, пояснение. */
    TEXT
}
